/* Сформировать прямоугольную матрицу на основе данных, вводимых пользователем.
 * Вывести транспонированную матрицу в консоль в табличном виде.
 */

// Ввод размеров матрицы
const rows = parseInt(prompt("Введите число строк:"));
const cols = parseInt(prompt("Введите число столбцов:"));

// Ввод элементов матрицы
const matrix = [];
for (let i = 0; i < rows; i++) {
  matrix[i] = [];
  for (let j = 0; j < cols; j++) {
    matrix[i][j] = parseInt(prompt(`Введите элемент [${i}][${j}]:`));
  }
}

// Транспонирование матрицы
const transpose = [];
for (let j = 0; j < cols; j++) {
  transpose[j] = [];
  for (let i = 0; i < rows; i++) {
    transpose[j][i] = matrix[i][j];
  }
}

// Вывод транспонированной матрицы
console.log("Транспонированная матрица:");
for (let i = 0; i < cols; i++) {
  console.log(transpose[i].join(" "));
}

/* Test data
2 3
-----
1 2 3
4 5 6
-----
1 4
2 5
3 6
==========
3 2
-----
7 8
9 10
11 12
-----
7 9 11
8 10 12
==========
1 4
-----
13 14 15 16
-----
13
14
15
16
==========
4 1
-----
17
18
19
20
-----
17 18 19 20
*/
